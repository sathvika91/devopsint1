pipeline {
    agent any
    
    tools {
        maven 'Maven 3.9.9'  // Ensure Maven tool is properly configured in Jenkins
    }
    
    stages {
        stage('Checkout SCM') {
            steps {
                checkout scm
            }
        }
        
        stage('Build & Test') {
            steps {
                echo 'Building and testing application using Jenkins tools...'
            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Deploying application using Jenkins...'
                // Add deployment steps here if required
            }
        }
        
        stage('Post Actions') {
            steps {
                echo 'Pipeline succeeded with Jenkins tools!'
            }
        }
    }
}
