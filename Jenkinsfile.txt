pipeline {
    agent any

    tools {
        // Use the configured Maven tool, replace 'Maven 3.9.9' with your installed version name if necessary
        maven 'Maven 3.9.9'
    }

    stages {
        stage('Checkout') {
            steps {
                // Clone your Git repository
                git 'https://github.com/sathvika91/devopsint1.git'
            }
        }
        
        stage('Build') {
            steps {
                // Run Maven build command
                echo 'Building...'
            }
        }

        stage('Test') {
            steps {
                // Run tests
                echo 'Running....'
            }
        }

        stage('Deploy') {
            steps {
                // Deploy application (update this with your actual deployment steps)
                echo 'Deploying the application...'
            }
        }
    }

    post {
        success {
            echo 'Build completed successfully.'
        }
        failure {
            echo 'Build failed.'
        }
    }
}
